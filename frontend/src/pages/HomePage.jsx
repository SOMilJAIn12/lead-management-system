import LeadForm from "../components/LeadForm";

/** HomePage simply hosts the Lead Capture Form. */
export default function HomePage() {
  return (
    <div className="py-10 px-4">
      <LeadForm />
    </div>
  );
}
